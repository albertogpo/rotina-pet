import {useState} from "react";import{sendPasswordReset,signIn,signUp}from"../services/api";
export function AuthScreen(){const[mode,setMode]=useState<"login"|"signup">("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[busy,setBusy]=useState(false),[message,setMessage]=useState(""),[error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMessage("");setError("");try{if(mode==="login")await signIn(email,password);else{const data=await signUp(email,password);if(!data.session)setMessage("Conta criada. Confira seu e-mail para confirmar o cadastro.")}}catch(err){setError(err instanceof Error?err.message:"Não foi possível continuar.")}finally{setBusy(false)}}async function reset(){if(!email.trim()){setError("Digite seu e-mail primeiro.");return}setBusy(true);setError("");setMessage("");try{await sendPasswordReset(email.trim());setMessage("Enviamos as instruções de recuperação para o seu e-mail.")}catch(err){setError(err instanceof Error?err.message:"Não foi possível enviar o e-mail.")}finally{setBusy(false)}}return <main className="center-page"><section className="auth-card"><div className="brand-mark">🐾</div><p className="eyebrow">Rotina Pet</p><h1>{mode==="login"?"Entrar":"Criar conta"}</h1><p className="muted">Acompanhe alimentação e peso de cada animal.</p><form onSubmit={submit} className="stack-form"><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} autoComplete="email"/></label><label>Senha<input type="password" required minLength={6} value={password} onChange={e=>setPassword(e.target.value)} autoComplete={mode==="login"?"current-password":"new-password"}/></label>{error&&<p className="error-box">{error}</p>}{message&&<p className="success-box">{message}</p>}<button className="primary-button" disabled={busy}>{busy?"Aguarde…":mode==="login"?"Entrar":"Criar conta"}</button></form><div className="auth-actions"><button className="link-button" onClick={()=>{setMode(mode==="login"?"signup":"login");setError("");setMessage("")}}>{mode==="login"?"Ainda não tenho conta":"Já tenho conta"}</button>{mode==="login"&&<button className="link-button" onClick={reset} disabled={busy}>Esqueci a senha</button>}</div></section></main>}
